# TODO: Optionally, add environment variables here, or use .zshrc instead
# This file is sourced from ~/.zshenv which is the only file not able to be
# stored in ~/.config/zsh because it defines $ZDOTDIR telling zsh where to
# find everything else.
# see zsh.sourceforge.net/Intro/intro_3.html
